export class KalimbaNote {
    name: string;
    height: string;
    sound: string;
  
    constructor(name: string, height: string, sound: string) {
      this.name = name;
      this.height = height;
      this.sound = sound;
    }
  }